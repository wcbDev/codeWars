/*function findMultiples(int,limit){
  let result = []
  
  for (let i = int; i<=limit ; i+=int)
    result.push(i)
    
  return result
}*/

function findMultiples(integer, limit) {
  let multiple = 1
  let results = []
  while (integer * multiple <= limit) {
    results.push(integer * multiple)
    multiple++
  }
  return results
}
