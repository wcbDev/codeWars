function basicOp(operation, value1, value2)
{
  if(operation === '+') {
    return parseInt(value1) + parseInt(value2)
  }
  else if(operation === '-') {
    return value1 - value2
  }
  else if(operation === '*') {
    return value1 * value2
  }
  else{ 
    return value1 / value2
  }
}